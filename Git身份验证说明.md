# Git身份验证说明

## 🔐 为什么需要身份验证？

GitHub和Gitee等平台需要验证你的身份才能推送代码。

---

## 🚀 GitHub身份验证

### 方法1：使用Personal Access Token（推荐）⭐

**步骤**：

1. **创建Token**
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token" → "Generate new token (classic)"
   - 设置名称（如：Cursor Project）
   - 选择过期时间
   - 勾选权限：至少需要 `repo` 权限
   - 点击 "Generate token"
   - **重要**：复制Token（只显示一次）

2. **使用Token推送**
   ```bash
   git push -u origin main
   ```
   - 用户名：输入你的GitHub用户名
   - 密码：**输入Token**（不是GitHub密码）

3. **保存凭据（可选）**
   - Windows会提示保存凭据
   - 选择保存，下次就不需要输入了

### 方法2：使用Git Credential Manager

Git for Windows自带凭据管理器，会自动处理。

### 方法3：使用SSH密钥

```bash
# 生成SSH密钥
ssh-keygen -t ed25519 -C "your_email@example.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 添加到GitHub: Settings → SSH and GPG keys → New SSH key
```

然后使用SSH地址：
```bash
git remote set-url origin git@github.com:username/repo.git
```

---

## 🇨🇳 Gitee身份验证

### 方法1：使用账号密码（简单）

```bash
git push -u origin main
```
- 用户名：输入Gitee用户名
- 密码：输入Gitee密码

### 方法2：使用Personal Access Token

1. **创建Token**
   - 访问：https://gitee.com/profile/personal_access_tokens
   - 点击 "生成新令牌"
   - 设置名称和权限
   - 生成并复制Token

2. **使用Token**
   - 推送时，密码输入Token

### 方法3：使用SSH密钥

类似GitHub的SSH配置方法。

---

## 🔧 常见问题

### Q1: 推送时提示"Authentication failed"

**A**: 
1. 检查用户名和密码/Token是否正确
2. GitHub必须使用Token，不能使用密码
3. 清除保存的凭据后重试

### Q2: 如何清除保存的凭据？

**A**: 
```bash
# Windows凭据管理器
# 打开：控制面板 → 凭据管理器 → Windows凭据
# 找到git相关的凭据，删除即可

# 或使用Git命令
git credential-manager-core erase
```

### Q3: Token在哪里查看？

**A**: 
- GitHub: https://github.com/settings/tokens
- Gitee: https://gitee.com/profile/personal_access_tokens

**注意**：Token只显示一次，如果忘记需要重新创建。

### Q4: 如何测试连接？

**A**: 
```bash
# 测试SSH连接（如果使用SSH）
ssh -T git@github.com

# 测试HTTPS连接
git ls-remote origin
```

---

## 💡 推荐方案

### 个人项目（推荐）

- **GitHub**: 使用Personal Access Token
- **Gitee**: 使用账号密码或Token

### 工作项目

- 使用公司提供的Git服务器
- 按照公司IT部门的要求配置

---

## 📝 快速配置步骤

1. **创建远程仓库**（GitHub/Gitee）
2. **配置远程地址**：运行 `配置远程仓库.bat`
3. **身份验证**：
   - GitHub：创建Token，推送时使用Token作为密码
   - Gitee：直接使用账号密码
4. **推送代码**：运行 `同步到远程.bat`

---

## 🆘 需要帮助？

如果遇到问题：
1. 检查网络连接
2. 确认仓库地址正确
3. 确认身份验证信息正确
4. 查看详细错误信息
