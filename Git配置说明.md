# Git用户信息配置说明

## 🎯 为什么需要配置？

Git需要知道你是谁，这样在提交代码时会记录你的身份信息。

---

## 🚀 快速配置方法

### 方法1：使用批处理文件（最简单）⭐

**双击运行**：`配置Git用户信息.bat`

脚本会引导你输入：
- 你的姓名
- 你的邮箱

### 方法2：手动配置（命令行）

打开命令提示符或PowerShell，在项目目录下运行：

```bash
cd "p:\Cursor Project"

# 配置用户名称
git config user.name "你的姓名"

# 配置用户邮箱
git config user.email "你的邮箱"
```

**示例**：
```bash
git config user.name "张三"
git config user.email "zhangsan@example.com"
```

---

## 📋 配置类型说明

### 本地配置（只影响当前项目）

```bash
git config user.name "你的姓名"
git config user.email "你的邮箱"
```

- ✅ 只影响当前项目（`p:\Cursor Project`）
- ✅ 适合不同项目使用不同身份
- ❌ 需要在每个项目单独配置

### 全局配置（影响所有Git仓库）⭐推荐

```bash
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"
```

- ✅ 一次配置，所有项目都生效
- ✅ 适合个人使用
- ❌ 所有项目使用相同身份

---

## ✅ 检查配置是否成功

### 方法1：使用批处理文件

双击运行：`配置Git用户信息.bat`，会显示当前配置

### 方法2：使用命令行

```bash
# 查看用户名称
git config user.name

# 查看用户邮箱
git config user.email

# 查看所有配置
git config --list
```

---

## 🔍 配置示例

### 示例1：使用真实姓名和邮箱

```bash
git config --global user.name "Colin Lin"
git config --global user.email "colin.lin@company.com"
```

### 示例2：使用GitHub/Gitee账号邮箱

```bash
git config --global user.name "colinlin"
git config user.email "colinlin@users.noreply.github.com"
```

**注意**：GitHub/Gitee会提供隐私邮箱，格式如：`username@users.noreply.github.com`

---

## 📝 完整配置流程

### 首次使用Git

1. **安装Git**（如果还未安装）
   - 下载：https://git-scm.com/download/win
   - 或使用：`winget install Git.Git`

2. **配置用户信息**
   ```bash
   git config --global user.name "你的姓名"
   git config --global user.email "你的邮箱"
   ```

3. **验证配置**
   ```bash
   git config --list
   ```

4. **初始化项目仓库**
   - 双击运行：`初始化Git仓库.bat`

---

## 🆘 常见问题

### Q1: 应该使用什么邮箱？

**A**: 
- 如果使用GitHub：可以使用GitHub提供的隐私邮箱
- 如果使用Gitee：可以使用Gitee账号邮箱
- 工作项目：使用公司邮箱
- 个人项目：使用个人邮箱

### Q2: 配置后可以修改吗？

**A**: 可以，随时可以重新运行配置命令修改

### Q3: 本地配置和全局配置有什么区别？

**A**: 
- **本地配置**：只影响当前项目
- **全局配置**：影响所有Git仓库

如果项目目录下有本地配置，会优先使用本地配置。

### Q4: 如何查看当前使用的是哪个配置？

**A**: 
```bash
# 查看本地配置
git config user.name
git config user.email

# 查看全局配置
git config --global user.name
git config --global user.email

# 查看所有配置（包括系统配置）
git config --list --show-origin
```

### Q5: 如何删除配置？

**A**: 
```bash
# 删除本地配置
git config --unset user.name
git config --unset user.email

# 删除全局配置
git config --global --unset user.name
git config --global --unset user.email
```

---

## 💡 推荐配置

### 个人项目（推荐全局配置）

```bash
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"
```

### 工作项目（推荐本地配置）

```bash
cd "p:\Cursor Project"
git config user.name "工作用姓名"
git config user.email "工作邮箱"
```

---

## 📞 需要帮助？

如果遇到问题：
1. 检查Git是否已安装：`git --version`
2. 查看详细错误信息
3. 告诉我具体问题，我可以帮你解决
