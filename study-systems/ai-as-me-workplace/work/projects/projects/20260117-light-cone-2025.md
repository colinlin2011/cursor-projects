---
title: 光锥计划（Light-Cone-2025）迁移与UI迭代记录
date: 2026-01-17
---

## 背景与目标
- 目标：将 `Light-Cone-2025` 迁移到 `ai-as-me-workplace/projects/` 并在不破坏现有功能的前提下，增强星空视图、时间轴与整体科技感UI。
- 约束：保持 Supabase 连接逻辑不变，支持三视图（星空/列表/公司），并预置至少 100 个光子用于冷启动吸引。

## 已完成工作
- 项目迁移至 `study-systems/ai-as-me-workplace/projects/light-cone-2025/`。
- 更新项目名称为 `light-cone-2025`（`package.json`）。
- 新增时间轴组件 `components/TimeAxis.tsx`，用于 2015-2035 时间范围展示与筛选。
- 强化星空画布表现：右向左粒子流、星体视差流动、悬停命中修复与 HUD 扫描效果（`components/StarCanvas.tsx`）。
- 星空视图内集成时间轴，并同步时间范围滑块交互。
- 底部光子类型图例改为平铺紧凑布局（`components/TemplateLegend.tsx`）。
- 默认演示光子数量提升至 100 个（`app/page-new.tsx`）。
- 优化光子映射算法：按公司分带、稳定抖动、减少重叠（`utils/photonMapper.ts`）。
- 清理 `globals.css` 重复定义并增强基础主题表现。

## 关键文件与修改点
- `projects/light-cone-2025/components/TimeAxis.tsx`：新增时间轴组件。
- `projects/light-cone-2025/components/StarCanvas.tsx`：星空特效与时间轴集成。
- `projects/light-cone-2025/app/page-new.tsx`：布局与默认数据增强。
- `projects/light-cone-2025/components/TemplateLegend.tsx`：底部类型说明紧凑布局。
- `projects/light-cone-2025/utils/photonMapper.ts`：映射算法优化。
- `projects/light-cone-2025/app/globals.css`：样式清理与主题增强。

## 当前状态
- 前端页面已部署（用户暂不可访问），后端 Supabase 连接已验证。
- 代码结构已迁移并可继续迭代。

## 风险与注意事项
- 线上版本可能尚未同步最新迁移目录内容（需确认 Vercel 项目源路径是否更新）。
- 时间轴范围滑块交互依赖 `StarCanvas` 注入的 `onTimeRangeChange`。

## 后续建议
- 若需验证线上效果，可在本地运行并核对 Vercel 部署分支。
- 可继续优化：公司视图时间轴对齐、密集光子自动避让、移动端交互优化。
